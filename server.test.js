// import server from './server';
// const environment = process.env.NODE_ENV || 'test'
// const configuration = require('./knexfile')[environment]
// const database = require('knex')(configuration)

describe('server', () => {
    describe('Get', () => {
        it.skip('should say a equals b', () => {
            const a = true;
            const b = true;
            expect(a).toEqual(b)
        })
    })
})